<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="org.zerock.mapper.OrderMapper">

	<!-- 주문 상품 정보 -->
	<select id="getGoodsInfo" resultType="org.zerock.model.OrderPageItemDTO">
	
		select bookId, bookName, bookPrice, bookDiscount
		from vam_book where bookId = #{bookId}	
	
	</select>
	
	<select id="getOrderInfo" resultType="org.zerock.model.OrderItemDTO">
	
		select bookId, bookPrice, bookDiscount
		from vam_book where bookId = #{bookId}
	
	</select>
	
	<!-- 주문 테이블 등록 -->
	<insert id="enrollOrder">
	
		insert into vam_order(orderId, addressee, memberId, memberAddr1, memberAddr2, memberAddr3, orderState, deliveryCost, usePoint)
    	values(#{orderId}, #{addressee}, #{memberId}, #{memberAddr1}, #{memberAddr2}, #{memberAddr3}, '배송준비', #{deliveryCost}, #{usePoint})
	
	</insert>
	
	<!-- 주문 아이템 테이블 등록 -->
	<insert id="enrollOrderItem">
	
		insert into vam_orderItem(orderItemId, orderId, bookId, bookCount, bookPrice, bookDiscount, savePoint)
		values(seq_order_item.nextval, #{orderId}, #{bookId}, #{bookCount}, #{bookPrice}, #{bookDiscount}, #{savePoint})
	
	</insert>
	
	<!-- 주문 금액 차감 -->
	<update id="deductMoney">
	
		update book_member set money = #{money}, point = #{point} where memberid = #{memberId}
		
	</update>
	
	<!-- 주문 재고 차감 -->
	<update id="deductStock">
	
		update vam_book set bookStock = #{bookStock} where bookId = #{bookId}
	
	</update>
	
	<delete id="deleteOrderCart">
	
		delete from vam_cart where memberId = #{memberId} and bookId = #{bookId}
	
	</delete>
	
	<!-- 주문취소 -->
	<update id="orderCancle">
		update vam_order set orderState = '주문취소' where orderId = #{orderId}
	</update>	

	<!-- 주문 상품 정보(주문취소) -->
	<select id="getOrderItemInfo" resultType="org.zerock.model.OrderItemDTO">
	
		select * from vam_orderItem
		where orderId = #{orderId}
	
	</select>
	
	<!-- 주문 정보(주문 취소) -->
	<select id="getOrder" resultType="org.zerock.model.OrderDTO">
	
		select * from vam_order
		where orderId = #{orderId}
	
	</select>

  </mapper>